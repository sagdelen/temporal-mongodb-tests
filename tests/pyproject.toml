[project]
name = "temporal-e2e-tests"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "temporalio>=1.7.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-timeout>=2.3.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["."]
timeout = 120
